---
## [10:44] ADR-014 작성 — Phase 2c 소스 저장 경로 확정

**완료한 작업:**
- Phase 2c 설계 탐색: upload flow, MinIO 레이아웃, SCIP proto 구조, 파서, ci-lib 조사
- 설계 옵션 평가 (A: CI zip / B: Git clone / C: hybrid / D: SCIP proto text) → Option A 선정
- ADR-014 작성 및 `docs/ADR.md` append
- ADR-013 트레이드오프 말미에 "→ ADR-014 해제" 주석 추가

**변경된 파일:**
- `docs/ADR.md` — ADR-014 엔트리 추가, ADR-013 해제 주석 추가

**다음 단계:**
- Phase 2c 구현 브랜치 시작
  1. `schema.sql` 컬럼 추가 (`indexes.source_blob_key/sha256/bytes`, `symbols.body_*` 4개)
  2. `parser.ts` — `Occurrence.enclosing_range` 파싱 활성화
  3. `materialize.ts` COPY 컬럼 확장
  4. `minio.ts` — zip entry 추출 헬퍼 추가 (`yauzl`/`adm-zip`)
  5. `upload.ts` — `source` multipart part 수용 (CI 필수 / client 거부)
  6. `routes/sources.ts` 신규 — `GET /v1/sources/file`, `GET /v1/sources/symbol`
  7. MCP tool 2개 신규 + 테스트
  8. `ci-lib` Jenkins step zip 생성 추가

**참조:**
- ref: ADR-014 — CI source.zip 업로드로 read_symbol_body / read_file_range 구현 경로 확정
- ref: ADR-013 → ADR-014로 해제
---
