---
## [12:24] Phase 2c 구현 완료 — read_symbol_body / read_file_range

**완료한 작업:**
- `schema.sql` — `indexes.source_blob_key/sha256/bytes`, `symbols.body_*` 4개 컬럼 추가 (idempotent ALTER TABLE)
- `parser.ts` — `Occurrence.enclosing_range` 파싱 활성화, `ParsedSymbol.body_*` 필드 추가
- `materialize.ts` — symbols INSERT에 body_* 컬럼 18개 파라미터로 확장
- `minio.ts` — `putSourceBlob`, `getSourceBlob`, `extractZipEntry`, `getSourceZipEntry` 추가 (adm-zip 의존성 추가)
- `config.ts` — `MAX_SOURCE_SIZE_MB` env var 추가
- `upload.ts` — CI: source 필수 + DB 저장 / client: source 거부
- `server.ts` — multipart files 한도 2로 수정, sources 라우트 등록
- `routes/sources.ts` — `GET /v1/sources/file`, `GET /v1/sources/symbol` 신규
- `mcp-server/src/client.ts` — `SymbolBodyResult`, `FileRangeResult`, `FileRangeParams` 타입 + `readSymbolBody`, `readFileRange` 메서드
- `tools/read-symbol-body.ts`, `tools/read-file-range.ts` — 신규 MCP tool handler
- `mcp-server/src/server.ts` — `read_symbol_body`, `read_file_range` tool 등록
- `ci-lib/jenkins/vars/cmsIndex.groovy` — source.zip 생성 + -F source=@source.zip 업로드 추가

**변경된 파일 (핵심):**
- `packages/core-service/src/storage/schema.sql` — Phase 2c 마이그레이션 컬럼
- `packages/core-service/src/storage/minio.ts` — zip 헬퍼 추가
- `packages/core-service/src/routes/upload.ts` — source part 수용
- `packages/core-service/src/routes/sources.ts` — **신규**
- `packages/scip-processor/src/parser.ts` — enclosing_range 파싱
- `packages/scip-processor/src/materialize.ts` — body_* 컬럼 확장
- `packages/mcp-server/src/client.ts` — 새 타입/메서드
- `packages/mcp-server/src/tools/read-symbol-body.ts` — **신규**
- `packages/mcp-server/src/tools/read-file-range.ts` — **신규**
- `packages/ci-lib/jenkins/vars/cmsIndex.groovy` — source.zip 업로드

**테스트 결과:**
- scip-processor: 21/21 (신규 8개: parser 5 + materializer 3)
- core-service: 61/61 (신규 15개: upload 3 + sources 7 + minio-zip 5)
- mcp-server: 60/60 (신규 10개: read-symbol-body 5 + read-file-range 5)
- 전체: 142/142 통과, typecheck 0 errors

**다음 단계:**
- Docker/실제 Postgres+MinIO 환경에서 E2E 검증
- scip-typescript 지원 추가 (다음 v1 우선순위)
- ADR-015 (source retention/GC 정책) 설계

**참조:**
- ref: ADR-014 — Phase 2c 구현 완료
---

---
## [10:44] ADR-014 작성 — Phase 2c 소스 저장 경로 확정

**완료한 작업:**
- Phase 2c 설계 탐색: upload flow, MinIO 레이아웃, SCIP proto 구조, 파서, ci-lib 조사
- 설계 옵션 평가 (A: CI zip / B: Git clone / C: hybrid / D: SCIP proto text) → Option A 선정
- ADR-014 작성 및 `docs/ADR.md` append
- ADR-013 트레이드오프 말미에 "→ ADR-014 해제" 주석 추가

**변경된 파일:**
- `docs/ADR.md` — ADR-014 엔트리 추가, ADR-013 해제 주석 추가

**다음 단계:**
- Phase 2c 구현 브랜치 시작
  1. `schema.sql` 컬럼 추가 (`indexes.source_blob_key/sha256/bytes`, `symbols.body_*` 4개)
  2. `parser.ts` — `Occurrence.enclosing_range` 파싱 활성화
  3. `materialize.ts` COPY 컬럼 확장
  4. `minio.ts` — zip entry 추출 헬퍼 추가 (`yauzl`/`adm-zip`)
  5. `upload.ts` — `source` multipart part 수용 (CI 필수 / client 거부)
  6. `routes/sources.ts` 신규 — `GET /v1/sources/file`, `GET /v1/sources/symbol`
  7. MCP tool 2개 신규 + 테스트
  8. `ci-lib` Jenkins step zip 생성 추가

**참조:**
- ref: ADR-014 — CI source.zip 업로드로 read_symbol_body / read_file_range 구현 경로 확정
- ref: ADR-013 → ADR-014로 해제
---

---
## [13:38] Storage 대안 조사 문서 작성 — docs/research/storage-alternatives.md

**완료한 작업:**
- 코드 인텔리전스 스토리지 백엔드 업계 사례 조사 (WebSearch + WebFetch)
- 해외 7개 사례 분석: Sourcegraph(Zoekt), GitHub Blackbird, Google Kythe, Meta Glean, CodeQL, Cursor/Continue.dev/Cody, Zoekt 단독
- 국내 4개 Tech 블로그 조사 → 코드 인텔리전스 특화 공개 자료 없음 확인 및 명시
- 비교 매트릭스 (11개 시스템 × 8개 속성) 작성
- CMS 격차 분석 (FTS 품질·레포 수·동시 쓰기·AI 쿼리·멀티홉 CTE 임계점)
- ADR 후보 시나리오 4개 (v1 유지 / v2 pgvector / v3 전용 검색 엔진 / v4 그래프 DB) 작성

**변경된 파일:**
- `docs/research/storage-alternatives.md` — **신규** (600줄, 참고자료 29개 출처 링크)

**다음 단계:**
- 본 조사 결과를 바탕으로 pgvector(시나리오 B) 도입 여부 ADR-015 작성 검토
- E2E 검증: Docker Postgres+MinIO 환경에서 Phase 2c 실제 동작 확인 (`bash scripts/e2e.sh`)
- scip-typescript 지원 추가 (다음 v1 우선순위)

**참조:**
- ref: ADR-001 — PostgreSQL + MinIO 선택 근거 (이 조사는 v2+ 참조용)
- ref: ADR-003, ADR-004 — 현 스택 결정

**이슈/메모:**
- 국내 기업(NAVER/카카오/LINE/토스)의 코드 인텔리전스 특화 공개 자료는 2026-04-17 현재 존재하지 않음. 검색 인프라(Elasticsearch 사용 등) 관련 인접 자료는 있으나 코드 심볼 저장/검색 특화 자료는 없음.
---

---
## [15:01] ADR-015 작성 — Primary Store 유지 결정 및 점진 확장 정책 확정

**완료한 작업:**
- storage-direction-vector-graph-options-2026-04-17.md 검토 및 의견 정리
- 옵션 A(Postgres 유지) / B(벡터+그래프 primary) / C(Graphify projection) 비교 분석
- 결정: Primary store PostgreSQL + MinIO 유지, 벡터·그래프는 파생 projection으로 점진 추가
- ADR-014의 "ADR-015(Retention/GC)" 참조를 "ADR-016"으로 수정
- ADR-015 작성 및 docs/ADR.md append
- Task #1 ADR 번호 ADR-018로 갱신 (Leiden 클러스터링 → Phase 2 이후)

**변경된 파일:**
- `docs/ADR.md` — ADR-015 엔트리 추가, ADR-014 GC 참조 번호 수정

**다음 단계:**
- E2E 검증: `docker compose up -d postgres minio && bash scripts/e2e.sh`
- ADR-016 작성: source blob Retention/GC 정책
- scip-typescript 지원 추가 (다음 v1 우선순위)

**참조:**
- ref: ADR-015 — Primary Store 유지 결정
- ref: docs/research/storage-direction-vector-graph-options-2026-04-17.md
---

---
## [16:35] Phase A Step 1 완료 — architecture-plan + work-log 문서 불일치 4건 정리

**완료한 작업:**
- 아키텍처 리뷰 5개 이슈 + 문서 불일치 4건 코드 레벨 검증 (upload.ts, conflict.ts, sources.ts)
- 6개 논의 포인트 사용자 결정 완료 → plan 문서 기록
- `docs/architecture-plan.md §4.4` — OIDC → Bearer 정적 토큰, source multipart 필수(CI)/거부(client) 명시, 400 응답 추가
- `docs/architecture-plan.md §4.6` — Jenkins 예시: source.zip 생성 sh 블록 + `-F source=@source.zip` 추가
- `docs/architecture-plan.md §4.9` — MinIO 레이아웃: 실구현(`{org}/{name}/{commit}/{tool}.scip` + `source.zip`)으로 수정, ADR-014 참조
- `docs/work-log.md` — `read_symbol_body`/`read_file_range` 보류 3곳 → 구현 완료(ADR-014) 전환, 현재 상태 트리 + 테스트 커버리지 현황(142/142) 최신화

**변경된 파일:**
- `docs/architecture-plan.md` — §4.4/§4.6/§4.9 실구현 동기화
- `docs/work-log.md` — Phase 2c 완료 반영

**다음 단계:**
- Phase A Step 2: ADR-016 Part 1 초안 작성 (reaper/failed/reclaiming GC 정책 — Two-Phase Replace와 통합)
- Phase A Step 3: ADR-014 "O(1) 랜덤 액세스" 문구 완화
- Phase A Step 4/5: P0 스키마 + Two-Phase Replace 구현 (upload.ts + conflict.ts + reaper)

**참조:**
- ref: ADR-014 — Phase 2c 구현 실체 기준 문서 동기화
- ref: docs/reviews/architecture-review-2026-04-17.md — 문서 불일치 4건 출처
---

---
## [16:37] Phase A Step 2+3 완료 — ADR-016 Part 1 초안 + ADR-014 문구 수정

**완료한 작업:**
- ADR-016 Part 1 작성: uploading 좀비 reaper(60s)/failed blob 삭제(24h)/DB row 삭제(7일)/reclaiming 3-phase/MinIO lifecycle(1d) 정책 확정
- ADR-016 Part 2(branch head 보존 수치/non-head 개수/410 Gone)는 E2E 후 실측 기반으로 분리
- ADR-014 zip 비용 모델 표현 완화: "O(1) 랜덤 액세스" → 로컬 파일 시스템 기준 O(1) + 현구현은 MinIO 전량 다운로드 명시, ADR-020 캐시 검토 예고

**변경된 파일:**
- `docs/ADR.md` — ADR-016 Part 1 append, ADR-014 zip 비용 모델 수정

**다음 단계:**
- Phase A Step 4/5: P0 스키마 마이그레이션(schema.sql) + Two-Phase Replace 구현(upload.ts, conflict.ts, blob-gc.ts) — TDD

**참조:**
- ref: ADR-016 Part 1
- ref: ADR-014 (zip 비용 표현 수정)
---

---
## [20:38] Phase A-4/5 완료 — Two-Phase Replace 전체 테스트 GREEN

**완료한 작업:**
- 9개 route 테스트 파일의 `ON CONFLICT (repo_id, commit_sha, tool)` 에 partial index predicate `WHERE status NOT IN ('failed', 'reclaiming')` 추가 (10건 교체)
- `pnpm typecheck` 0 errors 확인
- 전체 core-service 테스트 64/64 GREEN (Postgres 미기동 시 DB 테스트는 graceful skip)

**변경된 파일:**
- `packages/core-service/test/routes/file-overview.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/dependencies.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/impact.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/symbol-references.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/search.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/sources.test.ts` — ON CONFLICT predicate 추가 (2건)
- `packages/core-service/test/routes/symbols.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/repos.test.ts` — ON CONFLICT predicate 추가
- `packages/core-service/test/routes/implementors.test.ts` — ON CONFLICT predicate 추가

**다음 단계:**
- 커밋 A (feat): `upload.ts` + `conflict.ts` + `schema.sql` + `upload.test.ts` — Two-Phase Replace 본체
- 커밋 B (test): 9개 `test/routes/*.test.ts` — partial index 조건 대응
- 커밋 C (docs): `.work-log/2026-04-17.md` + untracked `docs/research/`, `docs/reviews/` 처리
- Phase A-6: identifier_fallback ±10 정규화 구현

**참조:**
- ref: ADR-016 Part 1 — Two-Phase Replace 설계 근거
- ref: Plan buzzing-pondering-volcano.md — 이번 작업 계획

**이슈/메모:**
- Docker 미기동으로 Postgres 연결 불가 → DB 테스트는 skip 처리됨. E2E 검증은 Docker 기동 후 별도 진행 필요.
---

---
## [17:00] Phase A-4/5 진행 중단 — Two-Phase Replace 구현 50% 완료

**완료한 작업:**
- TDD RED 테스트 3개 작성 (`upload.test.ts` Two-Phase Replace atomicity describe)
  1. blob PUT 실패 시 index status='failed' (not 'pending')
  2. CI-replaces-client에서 blob 실패 시 구 index 보존
  3. 성공 시 구 index 삭제, 신 index status='pending'
- schema.sql 업데이트:
  - `status` DEFAULT `'uploading'`, CHECK에 `'uploading'`/`'reclaiming'` 추가
  - `status_transition_at TIMESTAMPTZ` 컬럼 추가
  - 기존 UNIQUE(repo_id, commit_sha, tool) 제거
  - Partial UNIQUE INDEX `idx_indexes_active_unique WHERE status NOT IN ('failed','reclaiming')` 추가
  - Phase A migration block 추가 (기존 DB idempotent 적용용)
- `conflict.ts` 업데이트:
  - DELETE 지연: old row를 `status='reclaiming'`으로 UPDATE → partial index에서 제외
  - `ConflictDecision` 타입에 `replaces?: string` 추가
- `upload.ts` 상태 머신 재설계:
  - Phase 1 INSERT `status='uploading'` → COMMIT
  - Phase 2 blob PUT 성공 → `phase2Client` BEGIN, DELETE old (if replaces), UPDATE `status='pending'`, COMMIT, pg_notify
  - Phase 2 blob PUT 실패 → UPDATE `status='failed'`, 구 index RESTORE `status='pending'`
- `upload.test.ts` beforeAll에 `runMigrations()` 추가 (Phase 2c + Phase A 스키마 전체 적용)
- 새 3개 테스트 포함 upload.test.ts 6/6 GREEN ✓

**현재 블로커 — 미완료:**
- **회귀 9건**: `ON CONFLICT (repo_id, commit_sha, tool)` 사용 중인 9개 테스트 파일이 실패
  - 원인: PostgreSQL `ON CONFLICT (cols)` 는 full UNIQUE constraint 필요; partial index는 타겟 불가
  - 영향 파일: `symbols.test.ts`, `sources.test.ts`, `repos.test.ts`, `implementors.test.ts`, `search.test.ts`, `symbol-references.test.ts`, `impact.test.ts`, `dependencies.test.ts`, `file-overview.test.ts`
  - 수정 방향: `ON CONFLICT (repo_id, commit_sha, tool) WHERE status NOT IN ('failed', 'reclaiming') DO UPDATE ...` 로 전환 (partial index 조건 명시)

**변경된 파일:**
- `packages/core-service/src/storage/schema.sql` — Phase A 마이그레이션 추가, status 확장
- `packages/core-service/src/services/conflict.ts` — DELETE → reclaiming UPDATE, replaces 반환
- `packages/core-service/src/routes/upload.ts` — 상태 머신 재설계 (uploading→pending|failed)
- `packages/core-service/test/routes/upload.test.ts` — 3개 새 테스트, beforeAll runMigrations

**다음 단계 (재개 시):**
1. 9개 테스트 파일 `ON CONFLICT` 수정: `WHERE status NOT IN ('failed', 'reclaiming')` 조건 추가
2. 전체 테스트 GREEN 확인
3. `pnpm typecheck` 0 errors 확인
4. work-log 업데이트 및 커밋
5. Phase A-6 (identifier_fallback ±10)으로 진행

**참조:**
- ref: ADR-016 Part 1 — Two-Phase Replace 설계 근거
- ref: Plan snug-wobbling-anchor.md — Phase A-4/5 실행 계획
---
---
## [21:01] Phase A-6 완료 — identifier_fallback ±10 라인 반환 구현

**완료한 작업:**
- `sources.ts:197-203` body span 분기 로직 수정: `enclosing_range` 부재 시 동일 라인 반환 버그 제거
- `Math.max(1, start_line - 10)` ~ `Math.min(totalLines, start_line + 10)` 범위로 교체
- ADR-014 설계(`±10 fallback`) 의도 복원
- 테스트 2건 추가: `identifier_fallback` 정상 반환, 파일 경계 cap 케이스

**변경된 파일:**
- `packages/core-service/src/routes/sources.ts` — body span if/else 분기 교체 (5 LOC)
- `packages/core-service/test/routes/sources.test.ts` — FALLBACK_SYMBOL / BOUNDARY_SYMBOL 상수 + INSERT 2건 + 테스트 2건 추가

**테스트 결과:**
- `pnpm --filter @cms/core-service test` → 66/66 GREEN (신규 2건 포함)
- `pnpm typecheck` → 0 errors

**다음 단계:**
- Phase A Step 7: `resolveCommit()` helper 추출 — `sources.ts`, `symbols.ts`, `file-overview.ts` 에 적용, commit 미지정 시 `default_branch` → `repo_head` fallback / 없으면 400

**참조:**
- ref: ADR-014 — `read_symbol_body` ±10 fallback 설계 원안
- ref: `snug-wobbling-anchor.md` 논의 포인트 4 (옵션 P 결정)
---

## [21:53] Plan A 완료 — branch 필수화 + repo_head 신뢰성 보장 (ADR-018)

**완료한 작업:**
- `routes/upload.ts:64-67` — `branch` 필수 검증 추가 (`!branch || branch.trim() === ""` → 400 missing_fields)
- `routes/upload.ts:155` — `branch ?? null` → `branch` (검증 후 안전)
- `scip-processor/src/materialize.ts:31` — `branch: string | null` → `branch: string`
- `scip-processor/src/materialize.ts:127-135` — `if (branch)` 가드 제거, repo_head UPSERT 무조건 실행
- `scip-processor/src/worker.ts:63` — DB 조회 타입 `branch: string | null` → `branch: string`
- `schema.sql:24` — `branch TEXT` → `branch TEXT NOT NULL`
- `schema.sql` Phase B migration block 추가 (backfill + NOT NULL + CHECK)
- `cmsIndex.groovy:38` — `GIT_BRANCH` 미설정 시 명시적 fail-fast
- `test/routes/upload.test.ts` — 신규 3건 (branch missing/empty/valid) + 기존 buildMultipart 8건에 `branch: "main"` 추가
- `test/routes/sources.test.ts:415-418` — `branch=NULL` → `branch='main'` (Phase B NOT NULL 대응)
- `scip-processor/test/materialize.test.ts`, `materialize.relationships.test.ts` — ON CONFLICT partial index predicate 추가
- `docs/ADR.md` — ADR-018 추가

**변경된 파일:**
- `packages/core-service/src/routes/upload.ts` — branch 검증
- `packages/core-service/src/storage/schema.sql` — Phase B migration
- `packages/scip-processor/src/materialize.ts` — signature + 가드 제거
- `packages/scip-processor/src/worker.ts` — 타입 변경
- `packages/ci-lib/jenkins/vars/cmsIndex.groovy` — fail-fast
- `packages/core-service/test/routes/upload.test.ts` — 신규 3건 + 기존 branch 추가
- `packages/core-service/test/routes/sources.test.ts` — branch=NULL 수정
- `packages/scip-processor/test/materialize.test.ts` — ON CONFLICT predicate
- `packages/scip-processor/test/materialize.relationships.test.ts` — ON CONFLICT predicate
- `docs/ADR.md` — ADR-018

**테스트 결과:**
- core-service: 74/74 GREEN (신규 3건 포함)
- scip-processor: 21/21 GREEN
- typecheck: 0 errors

**다음 단계 (Plan B):**
- `resolveCommit()` 시그니처에 `branch?: string` 추가
- `routes/sources.ts`, `routes/file-overview.ts` querystring에 `branch` 파싱 + 전달
- MCP tool 3종 zod에 `branch: z.string().optional()` 추가
- `mcp-server/src/client.ts` 파라미터/qs 확장
- 테스트: 각 라우트 `branch=feature` 시나리오

**참조:**
- ref: ADR-018 — indexes.branch NOT NULL + repo_head 갱신 단일화
- ref: ADR-017 — commit-less 조회 semantics (이번 작업의 전제)
---

## [21:34] Phase A-7 완료 — `resolveCommit()` helper 통합

**목적**: 리뷰 P1 — 세 엔드포인트의 commit 미지정 semantics 통일 (`repo_head(default_branch)` 기준)

**완료한 작업:**
- `packages/core-service/src/services/commit-resolver.ts` 신규 생성 — `parseRepo()` + `resolveCommit()` helper
- `packages/core-service/src/routes/sources.ts` — `/v1/sources/file`, `/v1/sources/symbol` 두 라우트 모두 `resolveCommit()` 적용. `/sources/symbol`은 1-query JOIN → 2-query 분리 구조로 재작성
- `packages/core-service/src/routes/file-overview.ts` — `/v1/files/overview` 라우트 `resolveCommit()` 적용
- `docs/ADR.md` — ADR-017 추가 (commit-less semantics 통일 결정 기록)

**변경된 파일:**
- `packages/core-service/src/services/commit-resolver.ts` (신규)
- `packages/core-service/src/routes/sources.ts`
- `packages/core-service/src/routes/file-overview.ts`
- `packages/core-service/test/routes/sources.test.ts` — `NEWER_COMMIT` 시딩 + `describe("commit omission semantics")` 블록 추가
- `packages/core-service/test/routes/file-overview.test.ts` — `NEWER_COMMIT_FO` 시딩 + commit-less regression 테스트 추가

**테스트 결과:**
- `pnpm --filter @cms/core-service test` → 70/70 GREEN (신규 4건 포함)
- `pnpm typecheck` → 0 errors

**다음 단계:**
- `upload.ts` `branch` 필수화 검토 — 현재 branch=NULL이면 `repo_head` 미채움 → commit-less 조회 실패. 별도 ADR + plan 필요.
- `branch` 쿼리 파라미터 추가 지원 (MCP client 포함) — 후속 확장.

**참조:**
- ref: ADR-017 — commit 미지정 조회 semantics 통일
- ref: `docs/reviews/architecture-review-2026-04-17.md` P1
---
